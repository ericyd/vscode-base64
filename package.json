{
  "name": "eric_base64",
  "displayName": "eric_base64",
  "description": "convert the active document to base64",
  "version": "0.0.1",
  "publisher": "test",
  "engines": {
    "vscode": "^1.11.0"
  },
  "categories": ["Other"],
  "activationEvents": ["onCommand:extension.base64"],
  "main": "./extension",
  "contributes": {
    "commands": [
      {
        "command": "extension.base64",
        "title": "base64"
      }
    ]
  },
  "scripts": {
    "postinstall": "node ./node_modules/vscode/bin/install",
    "test": "node ./node_modules/vscode/bin/test"
  },
  "devDependencies": {
    "vscode": "^1.0.0"
  }
}
